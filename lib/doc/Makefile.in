# Makefile.in generated by automake 1.11.1 from Makefile.am.
# @configure_input@

# Copyright (C) 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002,
# 2003, 2004, 2005, 2006, 2007, 2008, 2009  Free Software Foundation,
# Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@

VPATH = @srcdir@
pkgincludedir = $(includedir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkglibexecdir = $(libexecdir)/@PACKAGE@
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = @build@
host_triplet = @host@
target_triplet = @target@
DIST_COMMON = $(dist_bibliodoc_DATA) $(dist_cadoc_DATA) \
	$(dist_clipartdoc_DATA) $(dist_csdoc_DATA) $(dist_dadoc_DATA) \
	$(dist_declipartdoc_DATA) $(dist_dedoc_DATA) $(dist_doc_DATA) \
	$(dist_eldoc_DATA) $(dist_esclipartdoc_DATA) \
	$(dist_esdoc_DATA) $(dist_eudoc_DATA) \
	$(dist_frclipartdoc_DATA) $(dist_frdoc_DATA) \
	$(dist_gldoc_DATA) $(dist_hedoc_DATA) $(dist_hudoc_DATA) \
	$(dist_idclipartdoc_DATA) $(dist_iddoc_DATA) \
	$(dist_itclipartdoc_DATA) $(dist_itdoc_DATA) \
	$(dist_jaclipartdoc_DATA) $(dist_jadoc_DATA) \
	$(dist_nbdoc_DATA) $(dist_nldoc_DATA) $(dist_pldoc_DATA) \
	$(dist_ptdoc_DATA) $(dist_rodoc_DATA) $(dist_rudoc_DATA) \
	$(dist_skdoc_DATA) $(dist_sldoc_DATA) $(dist_svdoc_DATA) \
	$(dist_ukclipartdoc_DATA) $(dist_ukdoc_DATA) \
	$(dist_zhCNdoc_DATA) $(srcdir)/Makefile.am \
	$(srcdir)/Makefile.in $(top_srcdir)/config/common.am
subdir = lib/doc
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/config/lyxinclude.m4 \
	$(top_srcdir)/config/pkg.m4 $(top_srcdir)/config/qt4.m4 \
	$(top_srcdir)/config/spell.m4 $(top_srcdir)/m4/codeset.m4 \
	$(top_srcdir)/m4/gettext.m4 $(top_srcdir)/m4/glibc2.m4 \
	$(top_srcdir)/m4/glibc21.m4 $(top_srcdir)/m4/iconv.m4 \
	$(top_srcdir)/m4/intdiv0.m4 $(top_srcdir)/m4/intl.m4 \
	$(top_srcdir)/m4/intmax.m4 $(top_srcdir)/m4/inttypes-pri.m4 \
	$(top_srcdir)/m4/inttypes_h.m4 $(top_srcdir)/m4/lcmessage.m4 \
	$(top_srcdir)/m4/lib-ld.m4 $(top_srcdir)/m4/lib-link.m4 \
	$(top_srcdir)/m4/lib-prefix.m4 $(top_srcdir)/m4/lock.m4 \
	$(top_srcdir)/m4/longdouble.m4 $(top_srcdir)/m4/longlong.m4 \
	$(top_srcdir)/m4/nls.m4 $(top_srcdir)/m4/po.m4 \
	$(top_srcdir)/m4/printf-posix.m4 $(top_srcdir)/m4/progtest.m4 \
	$(top_srcdir)/m4/size_max.m4 $(top_srcdir)/m4/stdint_h.m4 \
	$(top_srcdir)/m4/uintmax_t.m4 $(top_srcdir)/m4/ulonglong.m4 \
	$(top_srcdir)/m4/visibility.m4 $(top_srcdir)/m4/wchar_t.m4 \
	$(top_srcdir)/m4/wint_t.m4 $(top_srcdir)/m4/xsize.m4 \
	$(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
mkinstalldirs = $(install_sh) -d
CONFIG_HEADER = $(top_builddir)/config.h
CONFIG_CLEAN_FILES =
CONFIG_CLEAN_VPATH_FILES =
AM_V_GEN = $(am__v_GEN_$(V))
am__v_GEN_ = $(am__v_GEN_$(AM_DEFAULT_VERBOSITY))
am__v_GEN_0 = @echo "  GEN   " $@;
AM_V_at = $(am__v_at_$(V))
am__v_at_ = $(am__v_at_$(AM_DEFAULT_VERBOSITY))
am__v_at_0 = @
SOURCES =
DIST_SOURCES =
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = f=`echo $$p | sed -e 's|^.*/||'`;
am__install_max = 40
am__nobase_strip_setup = \
  srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*|]/\\\\&/g'`
am__nobase_strip = \
  for p in $$list; do echo "$$p"; done | sed -e "s|$$srcdirstrip/||"
am__nobase_list = $(am__nobase_strip_setup); \
  for p in $$list; do echo "$$p $$p"; done | \
  sed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
  $(AWK) 'BEGIN { files["."] = "" } { files[$$2] = files[$$2] " " $$1; \
    if (++n[$$2] == $(am__install_max)) \
      { print $$2, files[$$2]; n[$$2] = 0; files[$$2] = "" } } \
    END { for (dir in files) print dir, files[dir] }'
am__base_list = \
  sed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
  sed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
am__installdirs = "$(DESTDIR)$(bibliodocdir)" "$(DESTDIR)$(cadocdir)" \
	"$(DESTDIR)$(clipartdocdir)" "$(DESTDIR)$(csdocdir)" \
	"$(DESTDIR)$(dadocdir)" "$(DESTDIR)$(declipartdocdir)" \
	"$(DESTDIR)$(dedocdir)" "$(DESTDIR)$(docdir)" \
	"$(DESTDIR)$(eldocdir)" "$(DESTDIR)$(esclipartdocdir)" \
	"$(DESTDIR)$(esdocdir)" "$(DESTDIR)$(eudocdir)" \
	"$(DESTDIR)$(frclipartdocdir)" "$(DESTDIR)$(frdocdir)" \
	"$(DESTDIR)$(gldocdir)" "$(DESTDIR)$(hedocdir)" \
	"$(DESTDIR)$(hudocdir)" "$(DESTDIR)$(idclipartdocdir)" \
	"$(DESTDIR)$(iddocdir)" "$(DESTDIR)$(itclipartdocdir)" \
	"$(DESTDIR)$(itdocdir)" "$(DESTDIR)$(jaclipartdocdir)" \
	"$(DESTDIR)$(jadocdir)" "$(DESTDIR)$(nbdocdir)" \
	"$(DESTDIR)$(nldocdir)" "$(DESTDIR)$(pldocdir)" \
	"$(DESTDIR)$(ptdocdir)" "$(DESTDIR)$(rodocdir)" \
	"$(DESTDIR)$(rudocdir)" "$(DESTDIR)$(skdocdir)" \
	"$(DESTDIR)$(sldocdir)" "$(DESTDIR)$(svdocdir)" \
	"$(DESTDIR)$(ukclipartdocdir)" "$(DESTDIR)$(ukdocdir)" \
	"$(DESTDIR)$(zhCNdocdir)"
DATA = $(dist_bibliodoc_DATA) $(dist_cadoc_DATA) \
	$(dist_clipartdoc_DATA) $(dist_csdoc_DATA) $(dist_dadoc_DATA) \
	$(dist_declipartdoc_DATA) $(dist_dedoc_DATA) $(dist_doc_DATA) \
	$(dist_eldoc_DATA) $(dist_esclipartdoc_DATA) \
	$(dist_esdoc_DATA) $(dist_eudoc_DATA) \
	$(dist_frclipartdoc_DATA) $(dist_frdoc_DATA) \
	$(dist_gldoc_DATA) $(dist_hedoc_DATA) $(dist_hudoc_DATA) \
	$(dist_idclipartdoc_DATA) $(dist_iddoc_DATA) \
	$(dist_itclipartdoc_DATA) $(dist_itdoc_DATA) \
	$(dist_jaclipartdoc_DATA) $(dist_jadoc_DATA) \
	$(dist_nbdoc_DATA) $(dist_nldoc_DATA) $(dist_pldoc_DATA) \
	$(dist_ptdoc_DATA) $(dist_rodoc_DATA) $(dist_rudoc_DATA) \
	$(dist_skdoc_DATA) $(dist_sldoc_DATA) $(dist_svdoc_DATA) \
	$(dist_ukclipartdoc_DATA) $(dist_ukdoc_DATA) \
	$(dist_zhCNdoc_DATA)
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
pkgdatadir = @pkgdatadir@
ACLOCAL = @ACLOCAL@
ALLOCA = @ALLOCA@
AMTAR = @AMTAR@
AM_CFLAGS = @AM_CFLAGS@
AM_CPPFLAGS = @AM_CPPFLAGS@
AM_CXXFLAGS = @AM_CXXFLAGS@
AM_DEFAULT_VERBOSITY = @AM_DEFAULT_VERBOSITY@
AM_LDFLAGS = @AM_LDFLAGS@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
BOOST_MT = @BOOST_MT@
BOOST_SEP = @BOOST_SEP@
BUILD_INCLUDED_LIBINTL = @BUILD_INCLUDED_LIBINTL@
CATOBJEXT = @CATOBJEXT@
CC = @CC@
CCDEPMODE = @CCDEPMODE@
CFLAGS = @CFLAGS@
CFLAG_VISIBILITY = @CFLAG_VISIBILITY@
CPP = @CPP@
CPPFLAGS = @CPPFLAGS@
CXX = @CXX@
CXXCPP = @CXXCPP@
CXXDEPMODE = @CXXDEPMODE@
CXXFLAGS = @CXXFLAGS@
CYGPATH_W = @CYGPATH_W@
DATADIRNAME = @DATADIRNAME@
DEFS = @DEFS@
DEPDIR = @DEPDIR@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
EGREP = @EGREP@
ENCHANT_CFLAGS = @ENCHANT_CFLAGS@
ENCHANT_LIBS = @ENCHANT_LIBS@
EXEEXT = @EXEEXT@
FRONTENDS = @FRONTENDS@
FRONTENDS_PROGS = @FRONTENDS_PROGS@
FRONTENDS_SUBDIRS = @FRONTENDS_SUBDIRS@
GENCAT = @GENCAT@
GLIBC2 = @GLIBC2@
GLIBC21 = @GLIBC21@
GMSGFMT = @GMSGFMT@
GMSGFMT_015 = @GMSGFMT_015@
GREP = @GREP@
HAVE_ASPRINTF = @HAVE_ASPRINTF@
HAVE_POSIX_PRINTF = @HAVE_POSIX_PRINTF@
HAVE_SNPRINTF = @HAVE_SNPRINTF@
HAVE_VISIBILITY = @HAVE_VISIBILITY@
HAVE_WPRINTF = @HAVE_WPRINTF@
INSTALL = @INSTALL@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
INSTOBJEXT = @INSTOBJEXT@
INTLBISON = @INTLBISON@
INTLLIBS = @INTLLIBS@
INTLOBJS = @INTLOBJS@
INTL_LIBTOOL_SUFFIX_PREFIX = @INTL_LIBTOOL_SUFFIX_PREFIX@
INTL_MACOSX_LIBS = @INTL_MACOSX_LIBS@
LDFLAGS = @LDFLAGS@
LIBICONV = @LIBICONV@
LIBINTL = @LIBINTL@
LIBMULTITHREAD = @LIBMULTITHREAD@
LIBOBJS = @LIBOBJS@
LIBPSAPI = @LIBPSAPI@
LIBPTH = @LIBPTH@
LIBS = 
LIBSHLWAPI = @LIBSHLWAPI@
LIBTHREAD = @LIBTHREAD@
LTLIBICONV = @LTLIBICONV@
LTLIBINTL = @LTLIBINTL@
LTLIBMULTITHREAD = @LTLIBMULTITHREAD@
LTLIBOBJS = @LTLIBOBJS@
LTLIBPTH = @LTLIBPTH@
LTLIBTHREAD = @LTLIBTHREAD@
LYX_DATE = @LYX_DATE@
LYX_DIR_VER = @LYX_DIR_VER@
LYX_MAJOR_VERSION = @LYX_MAJOR_VERSION@
LYX_MINOR_VERSION = @LYX_MINOR_VERSION@
LYX_RELEASE_LEVEL = @LYX_RELEASE_LEVEL@
LYX_RELEASE_PATCH = @LYX_RELEASE_PATCH@
LYX_USERDIR_VER = @LYX_USERDIR_VER@
MAINT = @MAINT@
MAKEINFO = @MAKEINFO@
MKDIR_P = @MKDIR_P@
MOC4 = @MOC4@
MSGFMT = @MSGFMT@
MSGFMT_015 = @MSGFMT_015@
MSGMERGE = @MSGMERGE@
MYTHES_LIBS = @MYTHES_LIBS@
OBJC = @OBJC@
OBJCDEPMODE = @OBJCDEPMODE@
OBJCFLAGS = @OBJCFLAGS@
OBJEXT = @OBJEXT@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_URL = @PACKAGE_URL@
PACKAGE_VERSION = @PACKAGE_VERSION@
PATH_SEPARATOR = @PATH_SEPARATOR@
PKG_CONFIG = @PKG_CONFIG@
POSUB = @POSUB@
PRI_MACROS_BROKEN = @PRI_MACROS_BROKEN@
PYTHON = @PYTHON@
PYTHON_EXEC_PREFIX = @PYTHON_EXEC_PREFIX@
PYTHON_PLATFORM = @PYTHON_PLATFORM@
PYTHON_PREFIX = @PYTHON_PREFIX@
PYTHON_VERSION = @PYTHON_VERSION@
QT4DIR = @QT4DIR@
QT4_CORE_CFLAGS = @QT4_CORE_CFLAGS@
QT4_CORE_INCLUDES = @QT4_CORE_INCLUDES@
QT4_CORE_LDFLAGS = @QT4_CORE_LDFLAGS@
QT4_CORE_LIB = @QT4_CORE_LIB@
QT4_CORE_LIBS = @QT4_CORE_LIBS@
QT4_CPPFLAGS = @QT4_CPPFLAGS@
QT4_FRONTEND_CFLAGS = @QT4_FRONTEND_CFLAGS@
QT4_FRONTEND_LIBS = @QT4_FRONTEND_LIBS@
QT4_INCLUDES = @QT4_INCLUDES@
QT4_LDFLAGS = @QT4_LDFLAGS@
QT4_LIB = @QT4_LIB@
QT4_VERSION = @QT4_VERSION@
RANLIB = @RANLIB@
RCC4 = @RCC4@
RPM_FRONTEND = @RPM_FRONTEND@
RPM_VERSION_SUFFIX = @RPM_VERSION_SUFFIX@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
SOCKET_LIBS = @SOCKET_LIBS@
STRIP = @STRIP@
UIC4 = @UIC4@
USE_INCLUDED_LIBINTL = @USE_INCLUDED_LIBINTL@
USE_NLS = @USE_NLS@
VERSION = @VERSION@
VERSION_INFO = @VERSION_INFO@
WOE32DLL = @WOE32DLL@
XGETTEXT = @XGETTEXT@
XGETTEXT_015 = @XGETTEXT_015@
XMKMF = @XMKMF@
X_CFLAGS = @X_CFLAGS@
X_EXTRA_LIBS = @X_EXTRA_LIBS@
X_LIBS = @X_LIBS@
X_PRE_LIBS = @X_PRE_LIBS@
abs_builddir = @abs_builddir@
abs_srcdir = @abs_srcdir@
abs_top_builddir = @abs_top_builddir@
abs_top_srcdir = @abs_top_srcdir@
ac_ct_CC = @ac_ct_CC@
ac_ct_CXX = @ac_ct_CXX@
ac_ct_OBJC = @ac_ct_OBJC@
am__include = @am__include@
am__leading_dot = @am__leading_dot@
am__quote = @am__quote@
am__tar = @am__tar@
am__untar = @am__untar@
bindir = @bindir@
build = @build@
build_alias = @build_alias@
build_cpu = @build_cpu@
build_os = @build_os@
build_vendor = @build_vendor@
builddir = @builddir@
datadir = @datadir@
datarootdir = @datarootdir@
docdir = $(pkgdatadir)/doc
dvidir = @dvidir@
exec_prefix = @exec_prefix@
host = @host@
host_alias = @host_alias@
host_cpu = @host_cpu@
host_os = @host_os@
host_vendor = @host_vendor@
htmldir = @htmldir@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localedir = @localedir@
localstatedir = @localstatedir@
lyx_devel_version = @lyx_devel_version@
mandir = @mandir@
mkdir_p = @mkdir_p@
oldincludedir = @oldincludedir@
pdfdir = @pdfdir@
pkgpyexecdir = @pkgpyexecdir@
pkgpythondir = @pkgpythondir@
prefix = @prefix@
program_suffix = @program_suffix@
program_transform_name = @program_transform_name@
psdir = @psdir@
pyexecdir = @pyexecdir@
pythondir = @pythondir@
real_pkgdatadir = @real_pkgdatadir@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
srcdir = @srcdir@
sysconfdir = @sysconfdir@
target = @target@
target_alias = @target_alias@
target_cpu = @target_cpu@
target_os = @target_os@
target_vendor = @target_vendor@
top_build_prefix = @top_build_prefix@
top_builddir = @top_builddir@
top_srcdir = @top_srcdir@
version_suffix = @version_suffix@
CLEANFILES = pch.h.gch
DISTCLEANFILES = pch.h.gch.dep *.orig *.rej *~ *.bak core
MAINTAINERCLEANFILES = $(srcdir)/Makefile.in
AM_INSTALL_DATA_FLAGS = $(INSTALL) -m 644 -D
AM_ETAGSFLAGS = --lang=c++
@LYX_BUILD_PCH_TRUE@PCH_SOURCE = $(srcdir)/pch.h
@LYX_BUILD_PCH_TRUE@PCH_FLAGS = -Winvalid-pch --include=$(PCH_SOURCE)
@LYX_BUILD_PCH_TRUE@PCH_FILE = ./pch.h.gch
SUFFIXES = .gch
@USE_INCLUDED_BOOST_FALSE@BOOST_INCLUDES = 
@USE_INCLUDED_BOOST_TRUE@BOOST_INCLUDES = -I$(top_srcdir)/boost
@USE_INCLUDED_BOOST_FALSE@BOOST_LIBS = $(BOOST_REGEX) $(BOOST_SIGNALS)
@USE_INCLUDED_BOOST_TRUE@BOOST_LIBS = $(top_builddir)/boost/liblyxboost.a
@USE_INCLUDED_BOOST_FALSE@BOOST_REGEX = -lboost_regex$(BOOST_MT)
@USE_INCLUDED_BOOST_FALSE@BOOST_SIGNALS = -lboost_signals$(BOOST_MT)
EXTRA_DIST = README.Documentation
dist_doc_DATA = \
	Additional.lyx \
	Customization.lyx \
	DummyTextDocument.txt \
	DummyDocument1.lyx \
	DummyDocument2.lyx \
	EmbeddedObjects.lyx \
	Formula-numbering.lyx \
	Intro.lyx \
	LaTeXConfig.lyx \
	LFUNs.lyx \
	Math.lyx \
	Shortcuts.lyx \
	Tutorial.lyx \
	UserGuide.lyx

bibliodocdir = $(docdir)/biblio
dist_bibliodoc_DATA = \
	biblio/alphadin.bst \
	biblio/LyXDocs.bib

clipartdocdir = $(docdir)/clipart
dist_clipartdoc_DATA = \
	clipart/escher-lsd.eps \
	clipart/mobius.eps \
	clipart/platypus.eps \
	clipart/Abstract.pdf \
	clipart/BoxInsetDefaultQt4.png \
	clipart/ChangesToolbar.png \
	clipart/ChildDocumentQt4.png \
	clipart/CommentNoteImageQt4.png \
	clipart/endnotes.pdf \
	clipart/ERT.png \
	clipart/ExternalMaterialQt4.png \
	clipart/ExtraToolbar.png \
	clipart/floatQt4.png \
	clipart/footnoteQt4.png \
	clipart/GreyedOutNoteImageQt4.png \
	clipart/labelQt4.png \
	clipart/LaTeX.png \
	clipart/LyXNoteImageQt4.png \
	clipart/macrobox.png \
	clipart/MacroToolbar.png \
	clipart/macrouse.png \
	clipart/referenceQt4.png \
	clipart/SpaceMarker.png \
	clipart/StandardToolbar.png \
	clipart/ToolbarEnvBox.png \
	clipart/ViewToolbar.png \
	clipart/without_fntright.pdf \
	clipart/with_fntright.pdf

cadocdir = $(docdir)/ca
dist_cadoc_DATA = \
	ca/Intro.lyx

csdocdir = $(docdir)/cs
dist_csdoc_DATA = \
	cs/Tutorial.lyx

dadocdir = $(docdir)/da
dist_dadoc_DATA = \
	da/Intro.lyx

dedocdir = $(docdir)/de
dist_dedoc_DATA = \
	de/Additional.lyx \
	de/Customization.lyx \
	de/DummyDocument1.lyx \
	de/DummyDocument2.lyx \
	de/DummyTextDocument.txt \
	de/EmbeddedObjects.lyx \
	de/Formelnummerierung.lyx \
	de/Intro.lyx \
	de/Math.lyx \
	de/Shortcuts.lyx \
	de/Tutorial.lyx \
	de/UserGuide.lyx

declipartdocdir = $(dedocdir)/clipart
dist_declipartdoc_DATA = \
	de/clipart/ERT.png \
	de/clipart/ExternesMaterialQt4.png \
	de/clipart/FussnoteQt4.png \
	de/clipart/GleitobjektQt4.png \
	de/clipart/GrauschriftNotizQt4.png \
	de/clipart/KommentarQt4.png \
	de/clipart/LyXNotizQt4.png \
	de/clipart/Marke.png \
	de/clipart/Querverweis.png \
	de/clipart/StandardBoxQt4.png \
	de/clipart/UnterdokumentQt4.png

eldocdir = $(docdir)/el
dist_eldoc_DATA = \
	el/Intro.lyx

esdocdir = $(docdir)/es
dist_esdoc_DATA = \
	es/Additional.lyx \
	es/Customization.lyx \
	es/DocumentoPostizo1.lyx \
	es/DocumentoPostizo2.lyx \
	es/DocumentoTextoPostizo.txt \
	es/EmbeddedObjects.lyx \
	es/Formula-numbering.lyx \
	es/Intro.lyx \
	es/Math.lyx \
	es/Shortcuts.lyx \
	es/Tutorial.lyx \
	es/UserGuide.lyx

esclipartdocdir = $(esdocdir)/clipart
dist_esclipartdoc_DATA = \
	es/clipart/ComentNotaImagenQt4.png \
	es/clipart/CuadroMinipagQt4.png \
	es/clipart/DocumentoHijoQt4.png \
	es/clipart/es_ERT.png \
	es/clipart/es_ToolbarEnvBox.png \
	es/clipart/etiquetaQt4.png \
	es/clipart/flotanteQt4.png \
	es/clipart/GrisNotaImagenQt4.png \
	es/clipart/MaterialExternoQt4.png \
	es/clipart/NotaLyXImagenQt4.png \
	es/clipart/notapieQt4.png \
	es/clipart/referenciaQt4.png \
	es/clipart/Resumen.pdf

eudocdir = $(docdir)/eu
dist_eudoc_DATA = \
	eu/Customization.lyx \
	eu/Additional.lyx \
	eu/Intro.lyx \
	eu/Tutorial.lyx \
	eu/UserGuide.lyx

frdocdir = $(docdir)/fr
dist_frdoc_DATA = \
	fr/Additional.lyx \
	fr/Customization.lyx \
	fr/DocumentBidon1.lyx \
	fr/DocumentBidon2.lyx \
	fr/DocumentTexteBidon.txt \
	fr/EmbeddedObjects.lyx \
	fr/Formula-numbering.lyx \
	fr/Intro.lyx \
	fr/Math.lyx \
	fr/Shortcuts.lyx \
	fr/Tutorial.lyx \
	fr/UserGuide.lyx

frclipartdocdir = $(frdocdir)/clipart
dist_frclipartdoc_DATA = \
	fr/clipart/BoxInsetDefaultQt4.png \
	fr/clipart/ChildDocumentQt4.png \
	fr/clipart/CommentNoteImageQt4.png \
	fr/clipart/GreyedOutNoteImageQt4.png \
	fr/clipart/LyXNoteImageQt4.png \
	fr/clipart/floatQt4.png \
	fr/clipart/footnoteQt4.png \
	fr/clipart/labelQt4.png

gldocdir = $(docdir)/gl
dist_gldoc_DATA = \
	gl/Intro.lyx \
	gl/Tutorial.lyx

hedocdir = $(docdir)/he
dist_hedoc_DATA = \
	he/Intro.lyx \
	he/Tutorial.lyx

hudocdir = $(docdir)/hu
dist_hudoc_DATA = \
	hu/Intro.lyx \
	hu/Tutorial.lyx

iddocdir = $(docdir)/id
dist_iddoc_DATA = \
	id/Intro.lyx \
	id/Shortcuts.lyx \
	id/Tutorial.lyx \
	id/UserGuide.lyx

idclipartdocdir = $(iddocdir)/clipart
dist_idclipartdoc_DATA = \
	id/clipart/id-contrib.png \
	id/clipart/id-footnote.png \
	id/clipart/id-lingkungan.png

itdocdir = $(docdir)/it
dist_itdoc_DATA = \
	it/Customization.lyx \
	it/Intro.lyx \
	it/Tutorial.lyx \
	it/UserGuide.lyx

itclipartdocdir = $(itdocdir)/clipart
dist_itclipartdoc_DATA = \
	it/clipart/it_footnoteQt4.png \
	it/clipart/it_referenceQt4.png

jadocdir = $(docdir)/ja
dist_jadoc_DATA = \
	ja/Additional.lyx \
	ja/Customization.lyx \
	ja/DummyDocument1.lyx \
	ja/DummyDocument2.lyx \
	ja/EmbeddedObjects.lyx \
	ja/Formula-numbering.lyx \
	ja/Intro.lyx \
	ja/LaTeXConfig.lyx \
	ja/Math.lyx \
	ja/Shortcuts.lyx \
	ja/Tutorial.lyx \
	ja/UserGuide.lyx

jaclipartdocdir = $(jadocdir)/clipart
dist_jaclipartdoc_DATA = \
	ja/clipart/ChildDocumentQt4.png \
	ja/clipart/ERT.png \
	ja/clipart/ExternalMaterialQt4.png \
	ja/clipart/floatQt4.png \
	ja/clipart/footnoteQt4.png \
	ja/clipart/referenceQt4.png \
	ja/clipart/ToolbarEnvBox.png

nldocdir = $(docdir)/nl
dist_nldoc_DATA = \
	nl/Intro.lyx \
	nl/Tutorial.lyx

nbdocdir = $(docdir)/nb
dist_nbdoc_DATA = \
	nb/Intro.lyx

pldocdir = $(docdir)/pl
dist_pldoc_DATA = \
	pl/Additional.lyx \
	pl/Intro.lyx \
	pl/Tutorial.lyx

ptdocdir = $(docdir)/pt
dist_ptdoc_DATA = \
	pt/Intro.lyx \
	pt/Tutorial.lyx

rodocdir = $(docdir)/ro
dist_rodoc_DATA = \
	ro/Intro.lyx

rudocdir = $(docdir)/ru
dist_rudoc_DATA = \
	ru/Intro.lyx \
	ru/Tutorial.lyx

skdocdir = $(docdir)/sk
dist_skdoc_DATA = \
	sk/Intro.lyx \
	sk/Tutorial.lyx \
	sk/UserGuide.lyx

sldocdir = $(docdir)/sl
dist_sldoc_DATA = \
	sl/Intro.lyx \
	sl/Tutorial.lyx

svdocdir = $(docdir)/sv
dist_svdoc_DATA = \
	sv/Intro.lyx \
	sv/Tutorial.lyx

ukdocdir = $(docdir)/uk
dist_ukdoc_DATA = \
	uk/Intro.lyx

ukclipartdocdir = $(ukdocdir)/clipart
dist_ukclipartdoc_DATA = \
	uk/clipart/FootnoteQT4.png

zhCNdocdir = $(docdir)/zh_CN
dist_zhCNdoc_DATA = \
	zh_CN/Intro.lyx \
	zh_CN/Tutorial.lyx

doc_files = \
	    $(dist_doc_DATA) \
	    $(dist_cadoc_DATA) \
	    $(dist_csdoc_DATA) \
	    $(dist_dadoc_DATA) \
	    $(dist_dedoc_DATA) \
	    $(dist_esdoc_DATA) \
	    $(dist_eudoc_DATA) \
	    $(dist_frdoc_DATA) \
	    $(dist_gldoc_DATA) \
	    $(dist_hedoc_DATA) \
	    $(dist_hudoc_DATA) \
	    $(dist_itdoc_DATA) \
	    $(dist_jadoc_DATA) \
	    $(dist_nbdoc_DATA) \
	    $(dist_nldoc_DATA) \
	    $(dist_pldoc_DATA) \
	    $(dist_ptdoc_DATA) \
	    $(dist_rodoc_DATA) \
	    $(dist_rudoc_DATA) \
	    $(dist_skdoc_DATA) \
	    $(dist_sldoc_DATA) \
	    $(dist_svdoc_DATA) \
	    $(dist_ukdoc_DATA) \
	    $(dist_zhCNdoc_DATA)

all: all-am

.SUFFIXES:
.SUFFIXES: .gch
$(srcdir)/Makefile.in: @MAINTAINER_MODE_TRUE@ $(srcdir)/Makefile.am $(top_srcdir)/config/common.am $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      ( cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh ) \
	        && { if test -f $@; then exit 0; else break; fi; }; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --foreign lib/doc/Makefile'; \
	$(am__cd) $(top_srcdir) && \
	  $(AUTOMAKE) --foreign lib/doc/Makefile
.PRECIOUS: Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure: @MAINTAINER_MODE_TRUE@ $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4): @MAINTAINER_MODE_TRUE@ $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(am__aclocal_m4_deps):
install-dist_bibliodocDATA: $(dist_bibliodoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(bibliodocdir)" || $(MKDIR_P) "$(DESTDIR)$(bibliodocdir)"
	@list='$(dist_bibliodoc_DATA)'; test -n "$(bibliodocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(bibliodocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(bibliodocdir)" || exit $$?; \
	done

uninstall-dist_bibliodocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_bibliodoc_DATA)'; test -n "$(bibliodocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(bibliodocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(bibliodocdir)" && rm -f $$files
install-dist_cadocDATA: $(dist_cadoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cadocdir)" || $(MKDIR_P) "$(DESTDIR)$(cadocdir)"
	@list='$(dist_cadoc_DATA)'; test -n "$(cadocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cadocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cadocdir)" || exit $$?; \
	done

uninstall-dist_cadocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_cadoc_DATA)'; test -n "$(cadocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(cadocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(cadocdir)" && rm -f $$files
install-dist_clipartdocDATA: $(dist_clipartdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(clipartdocdir)" || $(MKDIR_P) "$(DESTDIR)$(clipartdocdir)"
	@list='$(dist_clipartdoc_DATA)'; test -n "$(clipartdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(clipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(clipartdocdir)" || exit $$?; \
	done

uninstall-dist_clipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_clipartdoc_DATA)'; test -n "$(clipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(clipartdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(clipartdocdir)" && rm -f $$files
install-dist_csdocDATA: $(dist_csdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(csdocdir)" || $(MKDIR_P) "$(DESTDIR)$(csdocdir)"
	@list='$(dist_csdoc_DATA)'; test -n "$(csdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(csdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(csdocdir)" || exit $$?; \
	done

uninstall-dist_csdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_csdoc_DATA)'; test -n "$(csdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(csdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(csdocdir)" && rm -f $$files
install-dist_dadocDATA: $(dist_dadoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(dadocdir)" || $(MKDIR_P) "$(DESTDIR)$(dadocdir)"
	@list='$(dist_dadoc_DATA)'; test -n "$(dadocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(dadocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(dadocdir)" || exit $$?; \
	done

uninstall-dist_dadocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_dadoc_DATA)'; test -n "$(dadocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(dadocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(dadocdir)" && rm -f $$files
install-dist_declipartdocDATA: $(dist_declipartdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(declipartdocdir)" || $(MKDIR_P) "$(DESTDIR)$(declipartdocdir)"
	@list='$(dist_declipartdoc_DATA)'; test -n "$(declipartdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(declipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(declipartdocdir)" || exit $$?; \
	done

uninstall-dist_declipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_declipartdoc_DATA)'; test -n "$(declipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(declipartdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(declipartdocdir)" && rm -f $$files
install-dist_dedocDATA: $(dist_dedoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(dedocdir)" || $(MKDIR_P) "$(DESTDIR)$(dedocdir)"
	@list='$(dist_dedoc_DATA)'; test -n "$(dedocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(dedocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(dedocdir)" || exit $$?; \
	done

uninstall-dist_dedocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_dedoc_DATA)'; test -n "$(dedocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(dedocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(dedocdir)" && rm -f $$files
install-dist_docDATA: $(dist_doc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(docdir)" || $(MKDIR_P) "$(DESTDIR)$(docdir)"
	@list='$(dist_doc_DATA)'; test -n "$(docdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(docdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(docdir)" || exit $$?; \
	done

uninstall-dist_docDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_doc_DATA)'; test -n "$(docdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(docdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(docdir)" && rm -f $$files
install-dist_eldocDATA: $(dist_eldoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(eldocdir)" || $(MKDIR_P) "$(DESTDIR)$(eldocdir)"
	@list='$(dist_eldoc_DATA)'; test -n "$(eldocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(eldocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(eldocdir)" || exit $$?; \
	done

uninstall-dist_eldocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_eldoc_DATA)'; test -n "$(eldocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(eldocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(eldocdir)" && rm -f $$files
install-dist_esclipartdocDATA: $(dist_esclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(esclipartdocdir)" || $(MKDIR_P) "$(DESTDIR)$(esclipartdocdir)"
	@list='$(dist_esclipartdoc_DATA)'; test -n "$(esclipartdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(esclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(esclipartdocdir)" || exit $$?; \
	done

uninstall-dist_esclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_esclipartdoc_DATA)'; test -n "$(esclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(esclipartdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(esclipartdocdir)" && rm -f $$files
install-dist_esdocDATA: $(dist_esdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(esdocdir)" || $(MKDIR_P) "$(DESTDIR)$(esdocdir)"
	@list='$(dist_esdoc_DATA)'; test -n "$(esdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(esdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(esdocdir)" || exit $$?; \
	done

uninstall-dist_esdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_esdoc_DATA)'; test -n "$(esdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(esdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(esdocdir)" && rm -f $$files
install-dist_eudocDATA: $(dist_eudoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(eudocdir)" || $(MKDIR_P) "$(DESTDIR)$(eudocdir)"
	@list='$(dist_eudoc_DATA)'; test -n "$(eudocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(eudocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(eudocdir)" || exit $$?; \
	done

uninstall-dist_eudocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_eudoc_DATA)'; test -n "$(eudocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(eudocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(eudocdir)" && rm -f $$files
install-dist_frclipartdocDATA: $(dist_frclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(frclipartdocdir)" || $(MKDIR_P) "$(DESTDIR)$(frclipartdocdir)"
	@list='$(dist_frclipartdoc_DATA)'; test -n "$(frclipartdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(frclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(frclipartdocdir)" || exit $$?; \
	done

uninstall-dist_frclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_frclipartdoc_DATA)'; test -n "$(frclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(frclipartdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(frclipartdocdir)" && rm -f $$files
install-dist_frdocDATA: $(dist_frdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(frdocdir)" || $(MKDIR_P) "$(DESTDIR)$(frdocdir)"
	@list='$(dist_frdoc_DATA)'; test -n "$(frdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(frdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(frdocdir)" || exit $$?; \
	done

uninstall-dist_frdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_frdoc_DATA)'; test -n "$(frdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(frdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(frdocdir)" && rm -f $$files
install-dist_gldocDATA: $(dist_gldoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(gldocdir)" || $(MKDIR_P) "$(DESTDIR)$(gldocdir)"
	@list='$(dist_gldoc_DATA)'; test -n "$(gldocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gldocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gldocdir)" || exit $$?; \
	done

uninstall-dist_gldocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_gldoc_DATA)'; test -n "$(gldocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(gldocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(gldocdir)" && rm -f $$files
install-dist_hedocDATA: $(dist_hedoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(hedocdir)" || $(MKDIR_P) "$(DESTDIR)$(hedocdir)"
	@list='$(dist_hedoc_DATA)'; test -n "$(hedocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(hedocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(hedocdir)" || exit $$?; \
	done

uninstall-dist_hedocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_hedoc_DATA)'; test -n "$(hedocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(hedocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(hedocdir)" && rm -f $$files
install-dist_hudocDATA: $(dist_hudoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(hudocdir)" || $(MKDIR_P) "$(DESTDIR)$(hudocdir)"
	@list='$(dist_hudoc_DATA)'; test -n "$(hudocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(hudocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(hudocdir)" || exit $$?; \
	done

uninstall-dist_hudocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_hudoc_DATA)'; test -n "$(hudocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(hudocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(hudocdir)" && rm -f $$files
install-dist_idclipartdocDATA: $(dist_idclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(idclipartdocdir)" || $(MKDIR_P) "$(DESTDIR)$(idclipartdocdir)"
	@list='$(dist_idclipartdoc_DATA)'; test -n "$(idclipartdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(idclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(idclipartdocdir)" || exit $$?; \
	done

uninstall-dist_idclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_idclipartdoc_DATA)'; test -n "$(idclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(idclipartdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(idclipartdocdir)" && rm -f $$files
install-dist_iddocDATA: $(dist_iddoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(iddocdir)" || $(MKDIR_P) "$(DESTDIR)$(iddocdir)"
	@list='$(dist_iddoc_DATA)'; test -n "$(iddocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(iddocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(iddocdir)" || exit $$?; \
	done

uninstall-dist_iddocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_iddoc_DATA)'; test -n "$(iddocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(iddocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(iddocdir)" && rm -f $$files
install-dist_itclipartdocDATA: $(dist_itclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(itclipartdocdir)" || $(MKDIR_P) "$(DESTDIR)$(itclipartdocdir)"
	@list='$(dist_itclipartdoc_DATA)'; test -n "$(itclipartdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(itclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(itclipartdocdir)" || exit $$?; \
	done

uninstall-dist_itclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_itclipartdoc_DATA)'; test -n "$(itclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(itclipartdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(itclipartdocdir)" && rm -f $$files
install-dist_itdocDATA: $(dist_itdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(itdocdir)" || $(MKDIR_P) "$(DESTDIR)$(itdocdir)"
	@list='$(dist_itdoc_DATA)'; test -n "$(itdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(itdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(itdocdir)" || exit $$?; \
	done

uninstall-dist_itdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_itdoc_DATA)'; test -n "$(itdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(itdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(itdocdir)" && rm -f $$files
install-dist_jaclipartdocDATA: $(dist_jaclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(jaclipartdocdir)" || $(MKDIR_P) "$(DESTDIR)$(jaclipartdocdir)"
	@list='$(dist_jaclipartdoc_DATA)'; test -n "$(jaclipartdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(jaclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(jaclipartdocdir)" || exit $$?; \
	done

uninstall-dist_jaclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_jaclipartdoc_DATA)'; test -n "$(jaclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(jaclipartdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(jaclipartdocdir)" && rm -f $$files
install-dist_jadocDATA: $(dist_jadoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(jadocdir)" || $(MKDIR_P) "$(DESTDIR)$(jadocdir)"
	@list='$(dist_jadoc_DATA)'; test -n "$(jadocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(jadocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(jadocdir)" || exit $$?; \
	done

uninstall-dist_jadocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_jadoc_DATA)'; test -n "$(jadocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(jadocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(jadocdir)" && rm -f $$files
install-dist_nbdocDATA: $(dist_nbdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(nbdocdir)" || $(MKDIR_P) "$(DESTDIR)$(nbdocdir)"
	@list='$(dist_nbdoc_DATA)'; test -n "$(nbdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(nbdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(nbdocdir)" || exit $$?; \
	done

uninstall-dist_nbdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_nbdoc_DATA)'; test -n "$(nbdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(nbdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(nbdocdir)" && rm -f $$files
install-dist_nldocDATA: $(dist_nldoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(nldocdir)" || $(MKDIR_P) "$(DESTDIR)$(nldocdir)"
	@list='$(dist_nldoc_DATA)'; test -n "$(nldocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(nldocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(nldocdir)" || exit $$?; \
	done

uninstall-dist_nldocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_nldoc_DATA)'; test -n "$(nldocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(nldocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(nldocdir)" && rm -f $$files
install-dist_pldocDATA: $(dist_pldoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(pldocdir)" || $(MKDIR_P) "$(DESTDIR)$(pldocdir)"
	@list='$(dist_pldoc_DATA)'; test -n "$(pldocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(pldocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(pldocdir)" || exit $$?; \
	done

uninstall-dist_pldocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_pldoc_DATA)'; test -n "$(pldocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(pldocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(pldocdir)" && rm -f $$files
install-dist_ptdocDATA: $(dist_ptdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(ptdocdir)" || $(MKDIR_P) "$(DESTDIR)$(ptdocdir)"
	@list='$(dist_ptdoc_DATA)'; test -n "$(ptdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ptdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ptdocdir)" || exit $$?; \
	done

uninstall-dist_ptdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_ptdoc_DATA)'; test -n "$(ptdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(ptdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(ptdocdir)" && rm -f $$files
install-dist_rodocDATA: $(dist_rodoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(rodocdir)" || $(MKDIR_P) "$(DESTDIR)$(rodocdir)"
	@list='$(dist_rodoc_DATA)'; test -n "$(rodocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(rodocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(rodocdir)" || exit $$?; \
	done

uninstall-dist_rodocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_rodoc_DATA)'; test -n "$(rodocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(rodocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(rodocdir)" && rm -f $$files
install-dist_rudocDATA: $(dist_rudoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(rudocdir)" || $(MKDIR_P) "$(DESTDIR)$(rudocdir)"
	@list='$(dist_rudoc_DATA)'; test -n "$(rudocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(rudocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(rudocdir)" || exit $$?; \
	done

uninstall-dist_rudocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_rudoc_DATA)'; test -n "$(rudocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(rudocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(rudocdir)" && rm -f $$files
install-dist_skdocDATA: $(dist_skdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(skdocdir)" || $(MKDIR_P) "$(DESTDIR)$(skdocdir)"
	@list='$(dist_skdoc_DATA)'; test -n "$(skdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(skdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(skdocdir)" || exit $$?; \
	done

uninstall-dist_skdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_skdoc_DATA)'; test -n "$(skdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(skdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(skdocdir)" && rm -f $$files
install-dist_sldocDATA: $(dist_sldoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(sldocdir)" || $(MKDIR_P) "$(DESTDIR)$(sldocdir)"
	@list='$(dist_sldoc_DATA)'; test -n "$(sldocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(sldocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(sldocdir)" || exit $$?; \
	done

uninstall-dist_sldocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_sldoc_DATA)'; test -n "$(sldocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(sldocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(sldocdir)" && rm -f $$files
install-dist_svdocDATA: $(dist_svdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(svdocdir)" || $(MKDIR_P) "$(DESTDIR)$(svdocdir)"
	@list='$(dist_svdoc_DATA)'; test -n "$(svdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(svdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(svdocdir)" || exit $$?; \
	done

uninstall-dist_svdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_svdoc_DATA)'; test -n "$(svdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(svdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(svdocdir)" && rm -f $$files
install-dist_ukclipartdocDATA: $(dist_ukclipartdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(ukclipartdocdir)" || $(MKDIR_P) "$(DESTDIR)$(ukclipartdocdir)"
	@list='$(dist_ukclipartdoc_DATA)'; test -n "$(ukclipartdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ukclipartdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ukclipartdocdir)" || exit $$?; \
	done

uninstall-dist_ukclipartdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_ukclipartdoc_DATA)'; test -n "$(ukclipartdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(ukclipartdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(ukclipartdocdir)" && rm -f $$files
install-dist_ukdocDATA: $(dist_ukdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(ukdocdir)" || $(MKDIR_P) "$(DESTDIR)$(ukdocdir)"
	@list='$(dist_ukdoc_DATA)'; test -n "$(ukdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ukdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ukdocdir)" || exit $$?; \
	done

uninstall-dist_ukdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_ukdoc_DATA)'; test -n "$(ukdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(ukdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(ukdocdir)" && rm -f $$files
install-dist_zhCNdocDATA: $(dist_zhCNdoc_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(zhCNdocdir)" || $(MKDIR_P) "$(DESTDIR)$(zhCNdocdir)"
	@list='$(dist_zhCNdoc_DATA)'; test -n "$(zhCNdocdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(zhCNdocdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(zhCNdocdir)" || exit $$?; \
	done

uninstall-dist_zhCNdocDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_zhCNdoc_DATA)'; test -n "$(zhCNdocdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(zhCNdocdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(zhCNdocdir)" && rm -f $$files
tags: TAGS
TAGS:

ctags: CTAGS
CTAGS:


distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d "$(distdir)/$$file"; then \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -fpR $(srcdir)/$$file "$(distdir)$$dir" || exit 1; \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    cp -fpR $$d/$$file "$(distdir)$$dir" || exit 1; \
	  else \
	    test -f "$(distdir)/$$file" \
	    || cp -p $$d/$$file "$(distdir)/$$file" \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(DATA)
installdirs:
	for dir in "$(DESTDIR)$(bibliodocdir)" "$(DESTDIR)$(cadocdir)" "$(DESTDIR)$(clipartdocdir)" "$(DESTDIR)$(csdocdir)" "$(DESTDIR)$(dadocdir)" "$(DESTDIR)$(declipartdocdir)" "$(DESTDIR)$(dedocdir)" "$(DESTDIR)$(docdir)" "$(DESTDIR)$(eldocdir)" "$(DESTDIR)$(esclipartdocdir)" "$(DESTDIR)$(esdocdir)" "$(DESTDIR)$(eudocdir)" "$(DESTDIR)$(frclipartdocdir)" "$(DESTDIR)$(frdocdir)" "$(DESTDIR)$(gldocdir)" "$(DESTDIR)$(hedocdir)" "$(DESTDIR)$(hudocdir)" "$(DESTDIR)$(idclipartdocdir)" "$(DESTDIR)$(iddocdir)" "$(DESTDIR)$(itclipartdocdir)" "$(DESTDIR)$(itdocdir)" "$(DESTDIR)$(jaclipartdocdir)" "$(DESTDIR)$(jadocdir)" "$(DESTDIR)$(nbdocdir)" "$(DESTDIR)$(nldocdir)" "$(DESTDIR)$(pldocdir)" "$(DESTDIR)$(ptdocdir)" "$(DESTDIR)$(rodocdir)" "$(DESTDIR)$(rudocdir)" "$(DESTDIR)$(skdocdir)" "$(DESTDIR)$(sldocdir)" "$(DESTDIR)$(svdocdir)" "$(DESTDIR)$(ukclipartdocdir)" "$(DESTDIR)$(ukdocdir)" "$(DESTDIR)$(zhCNdocdir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	$(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	  install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	  `test -z '$(STRIP)' || \
	    echo "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'"` install
mostlyclean-generic:

clean-generic:
	-test -z "$(CLEANFILES)" || rm -f $(CLEANFILES)

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)
	-test . = "$(srcdir)" || test -z "$(CONFIG_CLEAN_VPATH_FILES)" || rm -f $(CONFIG_CLEAN_VPATH_FILES)
	-test -z "$(DISTCLEANFILES)" || rm -f $(DISTCLEANFILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
	-test -z "$(MAINTAINERCLEANFILES)" || rm -f $(MAINTAINERCLEANFILES)
clean: clean-am

clean-am: clean-generic mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic

dvi: dvi-am

dvi-am:

html: html-am

html-am:

info: info-am

info-am:

install-data-am: install-dist_bibliodocDATA install-dist_cadocDATA \
	install-dist_clipartdocDATA install-dist_csdocDATA \
	install-dist_dadocDATA install-dist_declipartdocDATA \
	install-dist_dedocDATA install-dist_docDATA \
	install-dist_eldocDATA install-dist_esclipartdocDATA \
	install-dist_esdocDATA install-dist_eudocDATA \
	install-dist_frclipartdocDATA install-dist_frdocDATA \
	install-dist_gldocDATA install-dist_hedocDATA \
	install-dist_hudocDATA install-dist_idclipartdocDATA \
	install-dist_iddocDATA install-dist_itclipartdocDATA \
	install-dist_itdocDATA install-dist_jaclipartdocDATA \
	install-dist_jadocDATA install-dist_nbdocDATA \
	install-dist_nldocDATA install-dist_pldocDATA \
	install-dist_ptdocDATA install-dist_rodocDATA \
	install-dist_rudocDATA install-dist_skdocDATA \
	install-dist_sldocDATA install-dist_svdocDATA \
	install-dist_ukclipartdocDATA install-dist_ukdocDATA \
	install-dist_zhCNdocDATA
	@$(NORMAL_INSTALL)
	$(MAKE) $(AM_MAKEFLAGS) install-data-hook
install-dvi: install-dvi-am

install-dvi-am:

install-exec-am:

install-html: install-html-am

install-html-am:

install-info: install-info-am

install-info-am:

install-man:

install-pdf: install-pdf-am

install-pdf-am:

install-ps: install-ps-am

install-ps-am:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-dist_bibliodocDATA uninstall-dist_cadocDATA \
	uninstall-dist_clipartdocDATA uninstall-dist_csdocDATA \
	uninstall-dist_dadocDATA uninstall-dist_declipartdocDATA \
	uninstall-dist_dedocDATA uninstall-dist_docDATA \
	uninstall-dist_eldocDATA uninstall-dist_esclipartdocDATA \
	uninstall-dist_esdocDATA uninstall-dist_eudocDATA \
	uninstall-dist_frclipartdocDATA uninstall-dist_frdocDATA \
	uninstall-dist_gldocDATA uninstall-dist_hedocDATA \
	uninstall-dist_hudocDATA uninstall-dist_idclipartdocDATA \
	uninstall-dist_iddocDATA uninstall-dist_itclipartdocDATA \
	uninstall-dist_itdocDATA uninstall-dist_jaclipartdocDATA \
	uninstall-dist_jadocDATA uninstall-dist_nbdocDATA \
	uninstall-dist_nldocDATA uninstall-dist_pldocDATA \
	uninstall-dist_ptdocDATA uninstall-dist_rodocDATA \
	uninstall-dist_rudocDATA uninstall-dist_skdocDATA \
	uninstall-dist_sldocDATA uninstall-dist_svdocDATA \
	uninstall-dist_ukclipartdocDATA uninstall-dist_ukdocDATA \
	uninstall-dist_zhCNdocDATA

.MAKE: install-am install-data-am install-strip

.PHONY: all all-am check check-am clean clean-generic distclean \
	distclean-generic distdir dvi dvi-am html html-am info info-am \
	install install-am install-data install-data-am \
	install-data-hook install-dist_bibliodocDATA \
	install-dist_cadocDATA install-dist_clipartdocDATA \
	install-dist_csdocDATA install-dist_dadocDATA \
	install-dist_declipartdocDATA install-dist_dedocDATA \
	install-dist_docDATA install-dist_eldocDATA \
	install-dist_esclipartdocDATA install-dist_esdocDATA \
	install-dist_eudocDATA install-dist_frclipartdocDATA \
	install-dist_frdocDATA install-dist_gldocDATA \
	install-dist_hedocDATA install-dist_hudocDATA \
	install-dist_idclipartdocDATA install-dist_iddocDATA \
	install-dist_itclipartdocDATA install-dist_itdocDATA \
	install-dist_jaclipartdocDATA install-dist_jadocDATA \
	install-dist_nbdocDATA install-dist_nldocDATA \
	install-dist_pldocDATA install-dist_ptdocDATA \
	install-dist_rodocDATA install-dist_rudocDATA \
	install-dist_skdocDATA install-dist_sldocDATA \
	install-dist_svdocDATA install-dist_ukclipartdocDATA \
	install-dist_ukdocDATA install-dist_zhCNdocDATA install-dvi \
	install-dvi-am install-exec install-exec-am install-html \
	install-html-am install-info install-info-am install-man \
	install-pdf install-pdf-am install-ps install-ps-am \
	install-strip installcheck installcheck-am installdirs \
	maintainer-clean maintainer-clean-generic mostlyclean \
	mostlyclean-generic pdf pdf-am ps ps-am uninstall uninstall-am \
	uninstall-dist_bibliodocDATA uninstall-dist_cadocDATA \
	uninstall-dist_clipartdocDATA uninstall-dist_csdocDATA \
	uninstall-dist_dadocDATA uninstall-dist_declipartdocDATA \
	uninstall-dist_dedocDATA uninstall-dist_docDATA \
	uninstall-dist_eldocDATA uninstall-dist_esclipartdocDATA \
	uninstall-dist_esdocDATA uninstall-dist_eudocDATA \
	uninstall-dist_frclipartdocDATA uninstall-dist_frdocDATA \
	uninstall-dist_gldocDATA uninstall-dist_hedocDATA \
	uninstall-dist_hudocDATA uninstall-dist_idclipartdocDATA \
	uninstall-dist_iddocDATA uninstall-dist_itclipartdocDATA \
	uninstall-dist_itdocDATA uninstall-dist_jaclipartdocDATA \
	uninstall-dist_jadocDATA uninstall-dist_nbdocDATA \
	uninstall-dist_nldocDATA uninstall-dist_pldocDATA \
	uninstall-dist_ptdocDATA uninstall-dist_rodocDATA \
	uninstall-dist_rudocDATA uninstall-dist_skdocDATA \
	uninstall-dist_sldocDATA uninstall-dist_svdocDATA \
	uninstall-dist_ukclipartdocDATA uninstall-dist_ukdocDATA \
	uninstall-dist_zhCNdocDATA

@LYX_BUILD_PCH_TRUE@sinclude ./pch.h.gch.dep

@LYX_BUILD_PCH_TRUE@*.o *.lo *.obj: $(PCH_FILE)

@LYX_BUILD_PCH_TRUE@$(PCH_FILE): $(PCH_SOURCE)
@LYX_BUILD_PCH_TRUE@	$(MAKE) PCH_FLAGS= pch-file

@LYX_BUILD_PCH_TRUE@pch-file:
@LYX_BUILD_PCH_TRUE@	$(CXXCOMPILE) -x c++-header -fPIC -DPIC $(PCH_SOURCE) -o $(PCH_FILE) \
@LYX_BUILD_PCH_TRUE@	-MT $(PCH_FILE)  -MD -MP -MF "./$(PCH_FILE).Tdep" \
@LYX_BUILD_PCH_TRUE@	&& mv "./$(PCH_FILE).Tdep" "./$(PCH_FILE).dep" \
@LYX_BUILD_PCH_TRUE@	|| rm "./$(PCH_FILE).Tdep"

install-data-hook:
	for i in $(doc_files); \
	do \
		if $(EGREP) -q "LYX_DIR_VER|LYX_USERDIR_VER" "$(DESTDIR)$(docdir)/$$i"; then \
			sed -e "s/LYX_DIR_VER/$(LYX_DIR_VER)/g" \
			    -e "s/LYX_USERDIR_VER/$(LYX_USERDIR_VER)/g" \
				"$(DESTDIR)$(docdir)/$$i" > "$(DESTDIR)$(docdir)/$$i.tmp"; \
			mv -f "$(DESTDIR)$(docdir)/$$i.tmp" "$(DESTDIR)$(docdir)/$$i"; \
			chmod 644 "$(DESTDIR)$(docdir)/$$i"; \
		fi; \
	done

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
