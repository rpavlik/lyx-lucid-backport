#!/bin/sh -e

if [ "$1" = configure ]; then
    texhash

    printf "Generating LyX default configuration..."
    (cd /usr/share/lyx && ./configure.py >/dev/null) || false
    echo "done."
fi

#DEBHELPER#
